name: Test Self-Hosted Runner

on:
  push: 
  workflow_dispatch:

jobs:
  test:
    runs-on: [ebpf-runners-cicd]
    steps:
      - uses: actions/checkout@v3  # This checks out your code
      
      - name: Test Runner
        run: |
          echo "Hello from self-hosted runner"
          hostname
          env
          ls -la  # This will list the files in your repository